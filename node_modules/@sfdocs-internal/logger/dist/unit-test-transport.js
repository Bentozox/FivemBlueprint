"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const winston_transport_1 = __importDefault(require("winston-transport"));
//
// Inherit from `winston-transport` so you can take advantage
// of the base functionality and `.exceptions.handle()`.
//
class UnitTestTransport extends winston_transport_1.default {
    constructor(opts) {
        super(opts);
    }
    log(info, callback) {
        setImmediate(() => {
            this.emit('logged', info);
        });
        if (info.level === 'error') {
            process.stderr.write(`[${info.level}] ${info.message}` + '\n');
        }
        else {
            process.stdout.write(`[${info.level}] ${info.message}` + '\n');
        }
        // Perform the writing to the remote service
        callback();
    }
}
exports.default = UnitTestTransport;
//# sourceMappingURL=unit-test-transport.js.map