"use strict";
/**
 * Remark plugin for including external files.
 * Most of it taken from : https://github.com/Symbitic/remark-include
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateSharedFile = void 0;
const path_1 = __importDefault(require("path"));
const fs_1 = require("fs");
/**
 *
 * @param tree
 * @param file
 * @param cwd
 * @param processor
 */
function validate(_, file) {
    const docPathWithName = file.history[0];
    if (!(0, fs_1.existsSync)(docPathWithName)) {
        return;
    }
    const isFileInSharedFolder = path_1.default.basename(path_1.default.dirname(docPathWithName)) === 'shared';
    if (isFileInSharedFolder && file.contents && file.contents.toString().trim().length == 0) {
        file.message(`${path_1.default.basename(docPathWithName)} in shared folder cannot be empty`);
    }
}
function validateSharedFile() {
    // Shared folder path is mandatory otherwise we will not include any file.
    return (tree, file) => {
        validate(tree, file);
    };
}
exports.validateSharedFile = validateSharedFile;
//# sourceMappingURL=sharedFolderPlugin.js.map